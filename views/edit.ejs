<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
        <link rel="stylesheet" href="/css/app.css">
    </head>
    <body>
        <h1>Edit Fruits Page</h1>
        <form class="input" action="/fruits/<%=fruit.id%>?_method=PUT" method="POST">
        <!--  NOTE: the form is pre-populated with values for the server-->
        User Id: <input type="text" name="userId" value="<%=fruit.userId%>"/><br>
		Name: <input type="text" name="name" value="<%=fruit.name%>"/><br/>
        Color: <input type="text" name="color" value="<%=fruit.color%>"/><br/>
        Is Ready To Eat: <input type="checkbox" name="readyToEat" value="<%=fruit.readyToEat%>"/><br/>
        <select name="season">
            <% for ( let i = 0; i < seasons.length; i++ ) { let selected = ( i == 0
            ) ? "selected" : ""; %>
            <option value="<%=seasons[i].id%>" selected
              ><%=seasons[i].name%>
            </option>
            <% } %>
          </select>
		<br/>
		<input class="button" type="submit" name="" value="Submit Changes"/>
        </form>
    </body>
</html>